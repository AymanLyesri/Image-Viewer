import { ImageRatioDirective } from './image-ratio.directive';

describe('ImageRatioDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRatioDirective();
    expect(directive).toBeTruthy();
  });
});
