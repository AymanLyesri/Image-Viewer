import { HiddenElementDirective } from './hidden-element.directive';

describe('HiddenElementDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenElementDirective();
    expect(directive).toBeTruthy();
  });
});
