import { NavScrollDirective } from './nav-scroll.directive';

describe('NavScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NavScrollDirective();
    expect(directive).toBeTruthy();
  });
});
