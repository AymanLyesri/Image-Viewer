import { CursorDirective } from './cursor.directive';

describe('CursorDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorDirective();
    expect(directive).toBeTruthy();
  });
});
