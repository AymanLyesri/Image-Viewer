import { ParallaxImageDirective } from './parallax-image.directive';

describe('ParallaxImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxImageDirective();
    expect(directive).toBeTruthy();
  });
});
